<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cardoc">

    <application
        android:allowBackup="true"
        android:icon="@drawable/abcde"
        android:label="@string/app_name"
        android:roundIcon="@drawable/abcde"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity android:name=".Main4Activity"></activity>
        <activity android:name=".Main3Activity" />
        <activity android:name=".Licences" />
        <activity android:name=".Insurance" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".AlarmReciver" />
        <receiver android:name=".AlarmReciverI" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>